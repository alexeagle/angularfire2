load("//tools:defaults.bzl", "ts_library")

ts_library(
    name = "storage",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    module_name = "angularfire2/storage",
    deps = [
        "//src/core",
        "@rxjs",
    ],
)
