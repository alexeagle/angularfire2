load("//tools:defaults.bzl", "ts_library", "jasmine_node_test")

ts_library(
    name = "firestore",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    module_name = "angularfire2/firestore",
    deps = [
        "//src/core",
        "@rxjs",
    ],
)
