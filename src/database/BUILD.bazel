load("//tools:defaults.bzl", "ts_library", "jasmine_node_test")

ts_library(
    name = "database",
    srcs = glob(
        ["**/*.ts"],
        exclude = ["**/*.spec.ts"],
    ),
    module_name = "angularfire2/database",
    deps = [
        "//src/core",
        "@rxjs",
    ],
)
